// Code generated by BobGen psql v0.42.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"strconv"
	"strings"
	"time"

	"github.com/google/uuid"
	"github.com/jaswdr/faker/v2"
	enums "github.com/o-ga09/vite-8-sample-app/internal/infra/dbgen/dbenums"
	"github.com/shopspring/decimal"
)

var defaultFaker = faker.New()

func random_decimal_Decimal(f *faker.Faker, limits ...string) decimal.Decimal {
	if f == nil {
		f = &defaultFaker
	}

	var precision int64 = 7
	var scale int64 = 3

	if len(limits) > 0 {
		precision, _ = strconv.ParseInt(limits[0], 10, 32)
	}

	if len(limits) > 1 {
		scale, _ = strconv.ParseInt(limits[1], 10, 32)
	}

	baseVal := f.Float32(10, -1, 1)
	for baseVal == -1 || baseVal == 0 || baseVal == 1 {
		baseVal = f.Float32(10, -1, 1)
	}

	precisionDecimal, _ := decimal.NewFromInt(10).PowInt32(int32(precision))
	val := decimal.
		NewFromFloat32(baseVal).
		Mul(precisionDecimal).
		Shift(int32(-1 * scale)).
		RoundDown(int32(scale))

	return val
}

func random_enums_AccountType(f *faker.Faker, limits ...string) enums.AccountType {
	if f == nil {
		f = &defaultFaker
	}

	var e enums.AccountType
	all := e.All()
	return all[f.IntBetween(0, len(all)-1)]
}

func random_enums_CategoryType(f *faker.Faker, limits ...string) enums.CategoryType {
	if f == nil {
		f = &defaultFaker
	}

	var e enums.CategoryType
	all := e.All()
	return all[f.IntBetween(0, len(all)-1)]
}

func random_enums_MemberRole(f *faker.Faker, limits ...string) enums.MemberRole {
	if f == nil {
		f = &defaultFaker
	}

	var e enums.MemberRole
	all := e.All()
	return all[f.IntBetween(0, len(all)-1)]
}

func random_enums_TransactionType(f *faker.Faker, limits ...string) enums.TransactionType {
	if f == nil {
		f = &defaultFaker
	}

	var e enums.TransactionType
	all := e.All()
	return all[f.IntBetween(0, len(all)-1)]
}

func random_string(f *faker.Faker, limits ...string) string {
	if f == nil {
		f = &defaultFaker
	}

	val := strings.Join(f.Lorem().Words(f.IntBetween(1, 5)), " ")
	if len(limits) == 0 {
		return val
	}
	limitInt, _ := strconv.Atoi(limits[0])
	if limitInt > 0 && limitInt < len(val) {
		val = val[:limitInt]
	}
	return val
}

func random_time_Time(f *faker.Faker, limits ...string) time.Time {
	if f == nil {
		f = &defaultFaker
	}

	year := time.Hour * 24 * 365
	min := time.Now().Add(-year)
	max := time.Now().Add(year)
	return f.Time().TimeBetween(min, max)
}

func random_uuid_UUID(f *faker.Faker, limits ...string) uuid.UUID {
	if f == nil {
		f = &defaultFaker
	}

	return uuid.New()
}
