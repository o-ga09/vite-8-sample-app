// Code generated by BobGen psql v0.42.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package queries

import (
	"context"
	"fmt"
	"strings"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	testutils "github.com/stephenafamo/bob/test/utils"
)

func TestGetCategoryExpenseSummary(t *testing.T) {
	t.Run("Base", func(t *testing.T) {
		var sb strings.Builder

		query := GetCategoryExpenseSummary(random_time_Time(nil), random_time_Time(nil), random_uuid_UUID(nil))

		if _, err := query.WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		if diff := cmp.Diff(getCategoryExpenseSummarySQL, sb.String()); diff != "" {
			t.Fatalf("unexpected result (-got +want):\n%s", diff)
		}
	})

	t.Run("Mod", func(t *testing.T) {
		var sb strings.Builder

		query := GetCategoryExpenseSummary(random_time_Time(nil), random_time_Time(nil), random_uuid_UUID(nil))

		if _, err := psql.Select(query).WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		queryDiff, err := testutils.QueryDiff(getCategoryExpenseSummarySQL, sb.String(), formatQuery)
		if err != nil {
			t.Fatal(err)
		}
		if queryDiff != "" {
			fmt.Println(sb.String())
			t.Fatalf("unexpected result (-got +want):\n%s", queryDiff)
		}
	})

	t.Run("Scanning", func(t *testing.T) {
		if testDB == nil {
			t.Skip("skipping test, no DSN provided")
		}

		ctxTx, cancel := context.WithCancel(t.Context())
		defer cancel()

		tx, err := testDB.Begin(ctxTx)
		if err != nil {
			t.Fatalf("Error starting transaction: %v", err)
		}

		defer func() {
			if err := tx.Rollback(ctxTx); err != nil {
				t.Fatalf("Error rolling back transaction: %v", err)
			}
		}()

		query, args, err := bob.Build(ctxTx, psql.Select(GetCategoryExpenseSummary(random_time_Time(nil), random_time_Time(nil), random_uuid_UUID(nil))))
		if err != nil {
			t.Fatal(err)
		}

		rows, err := tx.QueryContext(ctxTx, query, args...)
		if err != nil {
			t.Fatal(err)
		}
		defer rows.Close()

		columns, err := rows.Columns()
		if err != nil {
			t.Fatal(err)
		}

		if len(columns) != 3 {
			t.Fatalf("expected %d columns, got %d", 3, len(columns))
		}

		if columns[0] != "category_id" {
			t.Fatalf("expected column %d to be %s, got %s", 0, "category_id", columns[0])
		}

		if columns[1] != "category_name" {
			t.Fatalf("expected column %d to be %s, got %s", 1, "category_name", columns[1])
		}

		if columns[2] != "total_expense" {
			t.Fatalf("expected column %d to be %s, got %s", 2, "total_expense", columns[2])
		}
	})
}

func TestGetAccountBalanceSummary(t *testing.T) {
	t.Run("Base", func(t *testing.T) {
		var sb strings.Builder

		query := GetAccountBalanceSummary(random_time_Time(nil), random_uuid_UUID(nil))

		if _, err := query.WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		if diff := cmp.Diff(getAccountBalanceSummarySQL, sb.String()); diff != "" {
			t.Fatalf("unexpected result (-got +want):\n%s", diff)
		}
	})

	t.Run("Mod", func(t *testing.T) {
		var sb strings.Builder

		query := GetAccountBalanceSummary(random_time_Time(nil), random_uuid_UUID(nil))

		if _, err := psql.Select(query).WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		queryDiff, err := testutils.QueryDiff(getAccountBalanceSummarySQL, sb.String(), formatQuery)
		if err != nil {
			t.Fatal(err)
		}
		if queryDiff != "" {
			fmt.Println(sb.String())
			t.Fatalf("unexpected result (-got +want):\n%s", queryDiff)
		}
	})

	t.Run("Scanning", func(t *testing.T) {
		if testDB == nil {
			t.Skip("skipping test, no DSN provided")
		}

		ctxTx, cancel := context.WithCancel(t.Context())
		defer cancel()

		tx, err := testDB.Begin(ctxTx)
		if err != nil {
			t.Fatalf("Error starting transaction: %v", err)
		}

		defer func() {
			if err := tx.Rollback(ctxTx); err != nil {
				t.Fatalf("Error rolling back transaction: %v", err)
			}
		}()

		query, args, err := bob.Build(ctxTx, psql.Select(GetAccountBalanceSummary(random_time_Time(nil), random_uuid_UUID(nil))))
		if err != nil {
			t.Fatal(err)
		}

		rows, err := tx.QueryContext(ctxTx, query, args...)
		if err != nil {
			t.Fatal(err)
		}
		defer rows.Close()

		columns, err := rows.Columns()
		if err != nil {
			t.Fatal(err)
		}

		if len(columns) != 4 {
			t.Fatalf("expected %d columns, got %d", 4, len(columns))
		}

		if columns[0] != "account_id" {
			t.Fatalf("expected column %d to be %s, got %s", 0, "account_id", columns[0])
		}

		if columns[1] != "account_name" {
			t.Fatalf("expected column %d to be %s, got %s", 1, "account_name", columns[1])
		}

		if columns[2] != "account_type" {
			t.Fatalf("expected column %d to be %s, got %s", 2, "account_type", columns[2])
		}

		if columns[3] != "current_balance" {
			t.Fatalf("expected column %d to be %s, got %s", 3, "current_balance", columns[3])
		}
	})
}

func TestGetWorkspaceDashboard(t *testing.T) {
	t.Run("Base", func(t *testing.T) {
		var sb strings.Builder

		query := GetWorkspaceDashboard(random_uuid_UUID(nil), random_time_Time(nil), random_time_Time(nil))

		if _, err := query.WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		if diff := cmp.Diff(getWorkspaceDashboardSQL, sb.String()); diff != "" {
			t.Fatalf("unexpected result (-got +want):\n%s", diff)
		}
	})

	t.Run("Mod", func(t *testing.T) {
		var sb strings.Builder

		query := GetWorkspaceDashboard(random_uuid_UUID(nil), random_time_Time(nil), random_time_Time(nil))

		if _, err := psql.Select(query).WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		queryDiff, err := testutils.QueryDiff(getWorkspaceDashboardSQL, sb.String(), formatQuery)
		if err != nil {
			t.Fatal(err)
		}
		if queryDiff != "" {
			fmt.Println(sb.String())
			t.Fatalf("unexpected result (-got +want):\n%s", queryDiff)
		}
	})

	t.Run("Scanning", func(t *testing.T) {
		if testDB == nil {
			t.Skip("skipping test, no DSN provided")
		}

		ctxTx, cancel := context.WithCancel(t.Context())
		defer cancel()

		tx, err := testDB.Begin(ctxTx)
		if err != nil {
			t.Fatalf("Error starting transaction: %v", err)
		}

		defer func() {
			if err := tx.Rollback(ctxTx); err != nil {
				t.Fatalf("Error rolling back transaction: %v", err)
			}
		}()

		query, args, err := bob.Build(ctxTx, psql.Select(GetWorkspaceDashboard(random_uuid_UUID(nil), random_time_Time(nil), random_time_Time(nil))))
		if err != nil {
			t.Fatal(err)
		}

		rows, err := tx.QueryContext(ctxTx, query, args...)
		if err != nil {
			t.Fatal(err)
		}
		defer rows.Close()

		columns, err := rows.Columns()
		if err != nil {
			t.Fatal(err)
		}

		if len(columns) != 5 {
			t.Fatalf("expected %d columns, got %d", 5, len(columns))
		}

		if columns[0] != "workspace_id" {
			t.Fatalf("expected column %d to be %s, got %s", 0, "workspace_id", columns[0])
		}

		if columns[1] != "total_income" {
			t.Fatalf("expected column %d to be %s, got %s", 1, "total_income", columns[1])
		}

		if columns[2] != "total_expense" {
			t.Fatalf("expected column %d to be %s, got %s", 2, "total_expense", columns[2])
		}

		if columns[3] != "net_flow" {
			t.Fatalf("expected column %d to be %s, got %s", 3, "net_flow", columns[3])
		}

		if columns[4] != "transaction_count" {
			t.Fatalf("expected column %d to be %s, got %s", 4, "transaction_count", columns[4])
		}
	})
}
